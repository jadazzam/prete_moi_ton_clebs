<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h1>Add my dog</h1>

      <%= simple_form_for @dog do |f| %>
        <%= f.input :name %>
        <%= f.input :age %>
        <%= f.input :breed %>
        <%= f.input :gender %>
        <%= f.input :description %>
        <%= f.button :submit, class: 'btn btn-warning' %>
        <%= link_to "", new_dog_path, class: "btn btn-warning btn-lg" %>
      <% end %>
    </div>
  </div>
</div>

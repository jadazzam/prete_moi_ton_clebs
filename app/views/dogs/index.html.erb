<div class="container">
  <div class="row">
  <div class="row">
    <% @dogs.each do |dog| %>
            <%= link_to "#{dog.name}", dog_path(dog)%>
    <% end %>

    <%= link_to "Add your dog", new_dog_path, class: "btn btn-warning btn-lg" %>
    </div>
  </div>
</div>

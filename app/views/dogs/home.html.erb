<div class="banner" style="">
  <div class="banner-content">
    <h1>Prête moi ton Clebs!</h1>
    <p>Ton chien est seul aujourd'hui ?</p>
    <!-- <button class="button"><span>Je m'en occupe!</span></button> -->
    <%= link_to "Je m'en occupe!", new_user_registration_path, class: "btn btn-warning btn-lg" %>
  </div>
</div>

<div class=container">
  <div class="row">
    <% @dogs.each do |dog| %>
      <div class="col-xs-12 col-sm-4">
        <div class="card" style="background-image: url(<%= dog.photo.url %>);">
          <div class="card-category">Nearby</div>
          <div class="card-description">
            <div>
              <%= link_to "#{dog.name}", dog_path(dog)%>
              <p><%= dog.breed %></p>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
